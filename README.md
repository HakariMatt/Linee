# Linee

A simple line-based text editor (similar to ed) that I've made for a uni term project.

To build run make command.

When running the program you can provide a filename in the arguments and the file will be loaded. To see available commands enter command `h` in the command prompt.
